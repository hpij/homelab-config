host_id: pi4

access:
  type: ssh
  host: pi4

operator:
  user: pi
  sudo: passwordless

software:
  package_managers:
    apt: {}
  runtimes:
    docker:
      managed_workloads_owner: homelab-docker
  components:
    pihole: {}

management:
  homelab_update:
    mechanisms:
      - apt
      - pihole

