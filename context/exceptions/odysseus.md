# Odysseus exception

Odysseus is a local, source-built Docker stack on `yip-i3wm`; it is intentionally outside the
managed workload desired state in `homelab-docker`.

Its expected components are Odysseus, ChromaDB, SearXNG, and ntfy. Inspect its local source and
build process before any explicitly requested modification.

